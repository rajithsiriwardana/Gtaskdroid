<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ogto.taskOrganizer"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:debuggable="true">
        <activity
            android:name="ogto.interactions.EventListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ogto.interactions.EventEditActivity" android:label="@string/app_name"></activity>
        <activity android:name="ogto.interactions.TaskPreferences" android:label="@string/app_name"></activity>
        <receiver android:name="ogto.reminderHandler.OnAlarmReceiver"></receiver>
        <service android:name="ogto.reminderHandler.ReminderService"></service>
        <receiver android:name="ogto.reminderHandler.OnBootReceiver">
            	<intent-filter>
            	    <action android:name="android.intent.action.BOOT_COMPLETED"/>
            	</intent-filter>            
        </receiver>
    </application>

</manifest>